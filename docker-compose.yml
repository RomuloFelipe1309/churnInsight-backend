services:
  api:
    image: eclipse-temurin:25-jdk
    container_name: churn-insight-api-dev
    working_dir: /app

    volumes:
      - .:/app

    command: ./mvnw spring-boot:run

    ports:
      - "8080:8080"

    environment:
      SPRING_PROFILES_ACTIVE: mock